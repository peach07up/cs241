#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv)
{
	int* a = (int*) malloc(1);
	int* b = (int*) malloc(1);
	int* c = (int*) malloc(100);
	int* d = (int*) malloc(100);

	printf("a = %p\nb = %p\nc = %p\nd = %p\n", a,b,c,d);
}